name: Deploy Demo to GitHub Pagesname: Deploy Demo to GitHub Pages



on:on:

  push:    push:

    branches:        branches: [main]

      - main    workflow_dispatch: # Allow manual triggers

  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pagespermissions:

permissions:    contents: read

  contents: read    pages: write

  pages: write    id-token: write

  id-token: write

# Allow only one concurrent deployment

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.concurrency:

# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.    group: "pages"

concurrency:    cancel-in-progress: false

  group: "pages"

  cancel-in-progress: falsejobs:

    # Build job - just validate and upload pre-built files

jobs:    build:

  deploy:        runs-on: ubuntu-latest

    environment:        steps:

      name: github-pages            - name: Checkout

      url: ${{ steps.deployment.outputs.page_url }}              uses: actions/checkout@v3

    runs-on: ubuntu-latest

    steps:            - name: Check if demo dist exists

      - name: Checkout              run: |

        uses: actions/checkout@v5                  if [ -d "apps/demo/dist" ]; then

                    echo "✅ Demo dist folder found"

      - name: Setup Pages                    ls -la apps/demo/dist/

        uses: actions/configure-pages@v5                  else

                    echo "❌ Demo dist folder not found - need to build locally first"

      - name: Upload artifact                    echo "Run: pnpm install && pnpm run package:build && pnpm run demo:build"

        uses: actions/upload-pages-artifact@v3                    exit 1

        with:                  fi

          # Upload the pre-built demo dist folder

          path: './apps/demo/dist'            - name: Setup Pages

              uses: actions/configure-pages@v3

      - name: Deploy to GitHub Pages

        id: deployment            - name: Upload artifact

        uses: actions/deploy-pages@v4              uses: actions/upload-pages-artifact@v3
              with:
                  path: ./apps/demo/dist

    # Deployment job
    deploy:
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v3
